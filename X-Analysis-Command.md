# Xポスト分析自動化システム

## 使用方法
以下のURLをこのプロンプトに貼り付けるだけで、自動分析が完了します。

```
https://x.com/[ユーザー名]/status/[投稿ID]

```

---

## 時系列事実（最重要：断定厳禁）
- 以下の文脈を基に記述すること。ただし**そのまま使わず、すべて推定形・柔らかい表現に変換**せよ。
- 表現例：「〜が起因している可能性が高い」「〜が背景にあると推定される」「〜の影響を受けたと見られる」など

インスピレーション: エンゲージメント順によるトップ投稿
Xのアルゴリズムにより、上位に表示されたポストの為、個別の事実は特になし

---

## X-Event-Analyzer v2.0 (汎用型)

# 指令
与えられた「時系列事実」と「観測数値」を照合し、当該ポストの構造を以下の3項目で客観的に記述せよ。
過度な推測、道徳的批判、感情的形容詞は一切禁止する。
分析結果も含め断定的な表現は避ける。
ポストされた時間と実際に分析をした時間を明確に記載すること。
分析は必ず「オリジナル言語（原文）」の文脈に基づいて行い、翻訳によるニュアンスの欠落を考慮せよ。

## 1. ポストの論理構造と意図されたユーザー行動
入力された事実（事実関係、コンテンツの種類）と、ポストの終着点（結びの言葉、リンク、画像等）を照合せよ。
投稿者がユーザーに対して「どの機能（返信/拡散/保存/クリック）」を物理的・心理的に要求しているかを構造的に特定せよ。

## 2. エンゲージメントの物理的特性分析
通常のバズ（いいね優位型）の平均値と比較し、本ポストの以下の比率を算出・定義せよ。

- **Controversy Ratio (返信/いいね比)**: 反論・議論・対話の発生密度
- **Retention Density (表示数に対する読了・滞在の深度)**: 長文または画像構成による滞在時間の推計
- **Utility/Aspiration Score (ブックマーク率)**: 情報の保存価値、または後続参照の必要性

## 3. アルゴリズム上の生存戦略
Xのアルゴリズム（For You推奨ロジック）において、本ポストが「高評価」を維持し続ける要因を特定せよ。

- **滞在時間生成因子**: テキスト量、メディア（画像/動画）の構成、スレッドの有無
- **インタラクション誘導**: リプライ欄への誘導導線、または引用RTを誘発する「ツッコミどころ（余白）」の有無

---

## 実行手順

## ツール環境設定

### 画像解析ツールの立ち上げ手順
1. **ローカルサーバーの起動**
   ```bash
   cd image-analyzer
   python -m http.server 8000
   ```

2. **ブラウザでのアクセス**
   - http://localhost:8000 にアクセス
   - MobileNetモデルの読み込み完了を確認

3. **ディレクトリ構成**
   ```
   X-kaiseki/
   ├── image-analyzer/
   │   ├── index.html          # メインUI
   │   ├── js/
   │   │   └── image-analyzer-fixed.js  # 解析ロジック
   │   └── README.md          # ツール説明
   ├── X-Analysis-Command.md  # 分析コマンド
   ├── procedure.md            # 手順書
   └── archive/               # 過去事例
   ```

### 画像解析ツールの使用方法
- **直接入力**: 画像URLを直接入力して解析
- **MCP連携**: AIアシストによる自動サムネイル取得
- **対応フォーマット**: jpg, png, gif, webp, 動画サムネイル

### STEP 1: データ抽出（DOM分析）
指定されたURLを開き、ログインセッションが有効であることを確認する。

ポストの aria-label 属性、および投稿時間を取得し、以下の形式で一時保存せよ。
分析結果の記録時に、これらのデータがいつ時点の物なのかを明確にしておくこと。

```
views: (表示数)
replies: (返信数)
reposts: (リポスト数)
likes: (いいね数)
bookmarks: (ブックマーク数)
timestamp: (投稿日時)
```

#### メディアコンテンツの処理フロー
ポスト内に画像または動画が含まれる場合、以下のアプローチで処理せよ。

**1. 画像コンテンツの場合:**
- DOMから `<img>` 要素を全て抽出
- `src` 属性から画像URLを取得
- `alt` 属性から代替テキストを確認
- 画像サイズ（width/height）とフォーマットを記録
- 複数画像の場合は各画像を個別に分析対象とする

**2. 動画コンテンツの場合:**
- DOMから `<video>` 要素を特定
- `poster` 属性からサムネイル画像URLを取得
- 動画の再生時間（duration）を確認
- 動画フォーマットと画質情報を記録
- サムネイル画像を画像解析の入力として使用

**3. 動画サムネイル取得の優先順位:**
1. MCP経由での自動取得（AIアシスト時）
2. DOMセレクターによる直接抽出（`video[poster]`）
3. 手動取得（開発者ツール使用）

**4. メディア解析の統合:**
- 取得した画像/サムネイルURLを画像解析ツールに入力
- OCR、画像認識、ピクセル分析を実行
- メディア特性をエンゲージメント分析の補助情報として活用

**5. エラーハンドリング:**
- CORS制約により直接アクセス不可の場合はMCP経由にフォールバック
- 画像読み込み失敗時は代替URLパターンを試行
- メディアが削除されている場合は事実として記録

### STEP 2: 事実情報の整理（Contextual Facts）
以下の項目について、ページ内の情報および検索等から得られる客観的事実を箇条書きで整理せよ。

- 投稿者の属性（プロフィール、フォロワー数、認証状況）
- ポストの形式（長文テキスト、画像、動画、引用等）
- プロフィール参照時、前後の投稿内容も確認し、分析ターゲットのポストとの関連性が明確であれば分析の参考にすること
- 検索可能であれば投稿に至るまでの時系列事実（ニュース、過去の炎上、コンテンツの出典等）を確認すること

※不明な場合は「観測可能な範囲での事実」のみを記述。

### STEP 3: 分析の実行
上記の「X-Event-Analyzer v2.0」分析基準に従い、STEP 1とSTEP 2で得たデータをインプットとして分析を実行せよ。

**出力形式:** 分析結果は`template\structure_template.md`に沿って出力すること。

### STEP 4: 最終出力
以下のファイル名でレポートを生成せよ。

```
Case_[ID]_[アカウント名]_[日付].md
Case_[ID]_[アカウント名]_[日付].json
```

**重要:** 
1. まずMarkdown形式で分析レポートを作成
2. 次にJSON形式に変換
3. 両方のファイルを同じディレクトリに配置

---

## 引用リポスト用要約文（140文字以内）

分析完了後、以下の形式で引用リポスト用の要約文を作成せよ。

**注意:** 画像起こしの際には以下の要約は含めない（要約文のセクションに注釈として記述すること）

**形式:**
```
【Case #[ID] 構造分析】

概要: [ポストの核心的な構造と目的を簡潔に記述]
力学: [エンゲージメントを駆動する主要な力学を特定]
観測点: [データの集計結果ではなく、数値を生み出している構造的な違和感やアルゴリズムへの影響力が最大化されている箇所を特定]

詳細はアーカイブへ：[GitHub PagesのURL]
```
**URL構造:**
```
https://[ユーザー名].github.io/x-case/case.html?case=archive/[日付]/[ファイル名].md&id=[ケースID]
```

**例:**
```
https://shizuka-yuu.github.io/x-case/case.html?case=archive/2026-05-11/Case_005_okada_junichi_2026-05-11.md&id=Case_005_okada_junichi_2026-05-11.md
```

**観測点の記述要件:**
- データの集計結果ではなく、その数値を生み出している「構造的な違和感」を特定
- アルゴリズムへの影響力が最大化されている箇所を指摘
- 異常値や特異なパターンの構造的原因を分析

---

# 出力形式
アーカイブ用のレポート形式（archive/yyyy-mm-dd/）で出力せよ。

**ファイル配置:**
- `Case_[ID]_[アカウント名]_[日付].md` - 元のMarkdownレポート
- `Case_[ID]_[アカウント名]_[日付].json` - JSON形式の分析データ

**JSON形式の要件:**
- 有効なJSON形式で出力すること
- 数値データは数値型（文字列ではない）で記録すること
- 文字列データは文字列型で記録すること
- 分析内容に応じて柔軟に構造を設計してよい

**必須フィールド:**
- `case_id`: ケース番号（文字列）
- `target`: 分析対象情報（オブジェクト）
- `metrics`: エンゲージメント指標（オブジェクト）
- `promotion_score`: 構造的プロモーション度合い（数値、0-100）

**分析要件:**
末尾に、本ポストが「特定の目的（認知拡大、信頼回復、収益化、自己顕示等）」に向けた【構造的セルフプロモーションとして機能している可能性があれば、その度合い】を、数値と事実の乖離に基づきパーセンテージで示し、`promotion_score` に記録すること。
