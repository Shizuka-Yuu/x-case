# X-case リビルド引き継ぎ書

## 📋 セッション概要

### 日時
2026-05-12 21:37（セッション終了時点）

### 目的
X-caseプロジェクトのリビルド実装に向けた設計フェーズ完了と実装フェーズへの引き継ぎ

## 🎯 完成した仕様書

### 1. 基本仕様書
- **ファイル**: `リビルド.md`
- **内容**: システム全体の基本仕様、アーキテクチャ、機能要件
- **更新済み**: 拡張機能項目を反映

### 2. 拡張機能仕様書
- **ファイル**: `拡張機能仕様.md`
- **内容**: 新機能の詳細実装仕様
- **主要機能**:
  - ソート機能拡張（エンゲージメント数）
  - レーダーチャート（6データ可視化）
  - マトリクス分析（志向性 vs 影響力）
  - 動的背景画像（既存実装継承）

## 🏗️ 実装方針

### ハイブリッドアプローチ
- **継承要素**: 既存資産を再利用
  - 動的背景画像機能
  - 基本CSSデザイン
  - データ読み込みロジック
- **ゼロビルド要素**: 新規実装
  - 拡張機能全般
  - 新規HTML構造
  - 新規ソート機能

## 📊 技術仕様の要点

### 1. レーダーチャート
- **データ項目**: 6つ
  1. 複合スコア（論争比率×プロモーション度）
  2. 表示数（対数スケール）
  3. いいね数（対数スケール）
  4. リポスト数（対数スケール）
  5. 返信数（対数スケール）
  6. ブックマーク数（対数スケール）
- **ライブラリ**: Chart.js
- **実装場所**: case.html

### 2. マトリクス分析
- **軸構成**: 4次元
  - X軸: 志向性（プロモーション度）
  - Y軸: 影響力（複合スコア）
  - サイズ: 表示数（リーチ）
  - 色: コンテンツタイプ
- **チャートタイプ**: bubble
- **実装場所**: case.html

### 3. 複合スコア計算
```javascript
// 論争比率を0-100に正規化し、プロモーション度と複合化
const normalizedControversy = Math.min(controversyRatio * 50, 100);
const compositeScore = (normalizedControversy + promotionScore) / 2;
```

## 🎨 デザイン仕様

### 継承要素
- **カラースキーム**: #667eea（青紫）と #764ba2（紫）のグラデーション
- **カードデザイン**: 角丸10px、シャドウ、ホバー効果
- **タイポグラフィ**: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto

### 動的背景画像
- **既存実装**: backup/case.jsの`updateBackgroundByPromotionScore()`を継承
- **分類**: low(0-19), normal(20-39), mid(40-69), high(70+)
- **トランジション**: 0.8秒アニメーション

## 📁 ファイル構成

### 既存ファイル（継承）
```
backup/
├── index.html          # トップページ構造
├── case.html           # 個別ページ構造
├── config.js           # 設定ファイル
├── index.js           # トップページロジック
├── case.js            # 個別ページロジック
├── styles.css         # 共通スタイル
└── case-styles.css   # 個別ページスタイル
```

### 新規作成ファイル
```
js/
├── chart-utils.js     # チャート関連ユーティリティ
└── data-utils.js     # データ処理ユーティリティ（拡張）
```

## 🚀 実装フェーズ優先順位

### フェーズ1: 基盤整備
1. Chart.jsライブラリ導入
2. 既存CSSバックアップ
3. 新規HTML構造追加

### フェーズ2: 機能実装
1. ソート機能拡張
2. レーダーチャート実装
3. マトリクス分析実装
4. 動的背景画像統合

### フェーズ3: データ連携
1. JSONデータ読み込み拡張
2. チャートデータ正規化
3. エラーハンドリング

### フェーズ4: テストと最適化
1. レスポンシブ検証
2. パフォーマンス最適化
3. クロスブラウザテスト

## ⚠️ 技術的注意点

### 1. データ構造
- 既存JSONフィールドの完全維持
- 新機能で必要なデータの有無確認
- エラーハンドリングの実装

### 2. パフォーマンス
- 初期読み込み: 3秒以内
- チャート描画: 1秒以内
- メモリ使用: 50MB以内

### 3. ブラウザ互換性
- Canvas対応必須
- ES6機能使用
- モダンブラウザ（Chrome 80+, Firefox 75+, Safari 13+, Edge 80+）

## 📝 次回セッションでの開始タスク

### 即時実施
1. Chart.js CDNリンク動作確認
2. 既存CSSファイルバックアップ
3. JSONデータ構造最終チェック

### 実装開始
1. フェーズ1: 基盤整備から着手
2. ハイブリッドアプローチで既存資産活用
3. 優先順位に従った段階的実装

## 🎯 成功基準

- 仕様書通りに全機能が実装完了
- 既存デザインと機能が完全維持
- 新機能が期待通り動作
- パフォーマンス要件を満たす
- レスポンシブデザイン対応

---

*この引き継ぎ書は設計フェーズの全成果と実装フェーズの方針を含んでいます。新規セッションでこの内容を基に実装を開始してください。*
